= develop playbooks, collections and containers
Nick Hardiman 
:source-highlighter: highlight.js
:revdate: 24-11-2023

Use public and private Ansible content collections to speed up playbook writing. 

Use a container to a playbook.

Write a content collection. 
Store it in Github, in Private Automation Hub, or in a custom container.

Build a custom container. 

todo 

* playbook
* role
* plugin filter
* plugin lookup
* plugin module

== dev pipeline 

. write a collection
. write an example playbook
. lint a playbook
. create a test environment
. run a playbook
. delete a test environment
. publish a collection

== execution environment

. use ansible image builder
. build a container
. publish a container
